name: flutter_project
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.2
  date_format: ^2.0.5
  plugin1:
    path: flutter-plugin/plugin1
  plugin2:
    path: flutter-plugin/plugin2
  models1:
    path: flutter-models/models1
  models2:
    path: flutter-models/models2

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:
  uses-material-design: true